# Copy this file to .env and fill in your real key (never commit .env)
# Used only for the optional AI-generated explanation feature (src/ai_explanation.py)
# The ML model does NOT need this key to make predictions.
ANTHROPIC_API_KEY=your_api_key_here
